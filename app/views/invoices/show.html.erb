<% @invoice.items.each do |item| %>
  <%= "Name: #{item.name}"  %> <br>
  <%= "Price: #{item.price}" %> <br>

  <% end %>

  <% unless @invoice.paid_at %>
    <%= link_to "Checkout Now", new_invoice_charge_path(@invoice) %>
  <% end %>